type Mutation {
  auth_local(email: String!, password: String!): AuthObj!
  refresh_token(refresh_token: String!): AuthObj!
}


type AuthObj {
  auth: Auth!
  user: User!
}

type Auth {
  token: String!
  refreshToken: String!
}